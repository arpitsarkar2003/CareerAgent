# Browser-safe values only. Never put Supabase keys or AI provider keys
# in this file — those live in apps/api/.env.

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
