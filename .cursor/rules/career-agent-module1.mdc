---
description: Career Agent Module 1 locked decisions and project boundaries
alwaysApply: true
---

# Career Agent — Module 1 locks

- Auth = Clerk. Supabase = Postgres + pgvector only. Web never talks to Supabase.
- Elevated DB client: `apps/api/db/` (`SUPABASE_SECRET_KEY`, legacy `SUPABASE_SERVICE_ROLE_KEY` fallback). Do not create `apps/api/supabase/` — it shadows the `supabase` PyPI package.
- `user_id` is text (Clerk id), not uuid FK to `auth.users`.
- Schema via `supabase/migrations/` only. Career Agent project ref `imypinqvbhdjavuotenh` — never apply SQL to howie.
- Probes: `GET /health`, `GET /health/db` (503 fail-closed, no secrets in body).
- Thin auth shell: public `/` with Sign in/up; protect `/dashboard` via
  `auth()` + `redirectToSignIn` in the page (Clerk `proxy.ts` / `clerkMiddleware`
  required). Web may hold server-only `CLERK_SECRET_KEY` for middleware
  (never `NEXT_PUBLIC_`). Fill `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` or Clerk
  runs keyless. API JWT verify starts with Module 2 product routes.
- Module 2 shipped (knowledge + dashboard shell). Do not start Module 3
  product work until following `docs/modules/03-ai-provider.md`.
  Roadmap: `docs/ROADMAP.md`.
